//! Timeseries dashboard view. Phase 3 placeholder; Phase 4 ports the mockup.
use dioxus::prelude::*;
use crate::views::ViewPlaceholder;

#[component]
pub fn TimeseriesDashboard() -> Element {
    rsx! { ViewPlaceholder { title: "Timeseries dashboard", note: "Timeseries placeholder — Phase 4." } }
}
