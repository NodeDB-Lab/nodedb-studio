//! Query editor view. Phase 3 placeholder; Phase 4 ports the mockup.
use dioxus::prelude::*;
use crate::views::ViewPlaceholder;

#[component]
pub fn Query() -> Element {
    rsx! { ViewPlaceholder { title: "Query", note: "SQL editor placeholder — Phase 4." } }
}
