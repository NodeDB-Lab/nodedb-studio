//! Spatial map view. Phase 3 placeholder; Phase 4 ports the mockup.
use dioxus::prelude::*;
use crate::views::ViewPlaceholder;

#[component]
pub fn SpatialView() -> Element {
    rsx! { ViewPlaceholder { title: "Spatial map", note: "Spatial map placeholder — Phase 4." } }
}
